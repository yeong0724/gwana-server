-- =====================================================================
-- 애드온 is_global 개념 제거
-- DB: gwana-local-db (MySQL 8.0)  /  2026-07-28
-- 배경: '전 상품 자동노출' 전역 애드온 패턴 폐기. 애드온은 오직 product_addon_map
--       매핑을 통해서만 상품에 노출된다(현업 정석: 상품별 명시 큐레이션).
-- 주의: 이미 is_global=1 이던 애드온은 자동노출이 사라지므로, 노출이 필요하면
--       해당 상품들에 대해 product_addon_map 을 명시적으로 채워야 한다.
-- =====================================================================

USE `gwana-local-db`;

ALTER TABLE `product_addon`
  DROP COLUMN `is_global`;
